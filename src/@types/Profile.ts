export type PlayerProfile = {
    id?: number;
    playerName: string;
    cardAtribute: Array<number>;
    cardProperties: Array<number>;
    PositiveBonus: number;
    NegativeBonus: number;
    lucky: number;
};

